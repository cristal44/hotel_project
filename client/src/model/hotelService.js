export default class HotelService {
    id
    name
    hour
    description

    constructor(id, name, hour, description) {
        this.id = id
        this.name = name
        this.hour = hour
        this.description = description
    }
}